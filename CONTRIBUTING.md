# Contributing

1. Create a focused branch.
2. Run `npm ci && npm run check`.
3. Do not commit credentials, phone numbers, message content, SQLite state, or downloaded media.
4. Keep changes small and test user-visible contracts rather than implementation presence.
